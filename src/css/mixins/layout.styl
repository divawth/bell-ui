layout-style(style)

    ui-set('flex', style['flex'])
    ui-set('background-color', style['background-color'])

ui-layout()

    ui-connect(
        hash: $layout-hash,
        each: layout-style
    )