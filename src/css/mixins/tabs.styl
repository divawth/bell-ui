size-style(style)

  {$selector-tabs-tab}
    ui-set('padding', style[ 'padding' ])
    ui-set('font-size', style[ 'size' ])
    ui-set('line-height', style[ 'line-height' ])

ui-tabs()
  ui-connect(
    hash: $tabs-bar-size-hash,
    each: size-style
  )