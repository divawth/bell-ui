badge-style(style)

  {$selector-badge-count}

    ui-set('background-color', style[ 'color' ])

  {$selector-badge-dot}

    ui-set('background-color', style[ 'color' ])

ui-badge()

  ui-connect(
    hash: $badge-brand-hash,
    each: badge-style
  )
