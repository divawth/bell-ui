button-reset()

    a&
        ui-set('color', $brand-text)
        text-decoration none

    button&

        background-color transparent
        outline 0

button-size(size)

    ui-set('font-size', size['font'])
    ui-set('line-height', size['line-height'])
    ui-set('padding', size['padding-vertical'] size['padding-horizontal'])

button-radius(radius)

    ui-set('border-radius', radius)

button-color(color, bg, borderColor)
    button&
        ui-set('color', color)
        ui-set('border-color', borderColor)
        if bg
            ui-set('background-color', bg)

button-border(border)
    ui-set('border', border)

button-style(style)

    button-color(
        style['color'],
        style['bg'],
        style['border-color']
    )

    {$selector-hover} {

        button-color(
            style['hover-color'],
            style['hover-bg'],
            style['hover-border-color']
        )

    }

    {$selector-active} {
        outline none

        button-color(
            style['active-color'],
            style['active-bg'],
            style['active-border-color']
        )
    }

    {$selector-focus} {
        outline none
        box-shadow 0 0 0 2px style['focus-border-color']
    }

    {$selector-disabled} {
        ui-set('cursor', not-allowed, true)
        button-color(
            style['disabled-color'],
            style['disabled-bg'],
            style['disabled-border-color']
        )
    }

ui-button()

    button-reset()

    ui-connect(
        hash: $button-size-hash,
        each: button-size
    )
    ui-connect(
        hash: $button-radius-hash,
        each: button-radius
    )
    ui-connect(
        hash: $button-hash,
        each: button-style
    )

    ui-connect(
        hash: $button-border-hash,
        each: button-border
    )
    > {$selector-text}

        font inherit

ui-buttons()

    ui-collection(
        selector-item: $selector-button,
        hash: $button-size-hash,
        each: @(size) {
            > {$selector-button} {
                button-size(size)
            }

            ui-connect(
                hash: $button-radius-hash,
                each: button-radius
            )
        }
    )