$radius-base = 4px

$radius-tiny = 1px

$radius-small = 2px

$radius-large = 8px

$radius-huge = 10px

$radius-circle = 50%

$radius-round = 32px

$radius-hash = {

    '': $radius-base,

    'tiny': $radius-tiny,
    'small': $radius-small,
    'large': $radius-large,
    'huge': $radius-huge,
    'circle': $radius-circle,
    'round': $radius-round

}

