$selector-avatar = $selector-prefix + '-avatar'
$selector-avatar-circle = $selector-avatar + '-circle'
$selector-avatar-image = $selector-avatar + '-image'
$selector-avatar-text = $selector-avatar + '-text'

$avatar-bg-color = #ccc

$avatar-size-hash = {
  default: {
    size: $height-default,
  },
  small: {
    size: $height-small,
  },
  large: {
    size: $height-large,
  }
}

