$selector-badge = $selector-prefix + '-badge'
$selector-badge-status = $selector-badge + '-status'
$selector-badge-ripple = $selector-badge + '-ripple'
$selector-badge-dot = $selector-badge + '-dot'
$selector-badge-text = $selector-badge + '-text'
$selector-badge-text-append = $selector-badge-text + '-append'

$badge-dot-size = 10px
$badge-status-dot-size = 6px
$badge-text-height = 20px
$badge-padding-horizontal = 7px

$badge-brand-hash = {
  primary: {
    color: $brand-primary
  },
  info: {
    color: $brand-info
  },
  error: {
    color: $brand-error
  },
  success: {
    color: $brand-success
  },
  warning: {
    color: $brand-warning
  }
}