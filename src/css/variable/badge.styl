$selector-badge = $selector-prefix + '-badge'
$selector-badge-status = $selector-badge + '-status'
$selector-badge-ripple = $selector-badge + '-ripple'
$selector-badge-content = $selector-badge + '-content'
$selector-badge-dot = $selector-badge + '-dot'
$selector-badge-text = $selector-badge + '-text'
$selector-badge-text-addon = $selector-badge-text + '-addon'
$selector-badge-text-only = $selector-badge-text + '-only'

$badge-dot-size = 8px
$badge-text-height = 20px
$badge-padding-horizontal = 6px

$badge-brand-hash = {
  'primary': {
    color: $brand-primary
  },
  'info': {
    color: $brand-info
  },
  'error': {
    color: $brand-error
  },
  'success': {
    color: $brand-success
  },
  'warning': {
    color: $brand-warning
  }
}