$selector-rate = $selector-prefix + '-rate'
$selector-rate-live = $selector-rate + '-live'
$selector-rate-dead = $selector-rate + '-dead'
$selector-rate-icon-full = $selector-rate + '-icon-full'
$selector-rate-icon-half = $selector-rate + '-icon-half'
$selector-rate-icon-active = $selector-rate + '-icon-active'
$selector-rate-text = $selector-rate + '-text'

$rate-icon-gap = 8px
$rate-text-font-size = 12px

$rate-icon-color = #e9e9e9
$rate-text-color = #515a6e

$rate-brand-hash = {
  primary: {
    color: $brand-primary
  },
  success: {
    color: $brand-success
  },
  error: {
    color: $brand-error
  },
  info: {
    color: $brand-info
  },
  warning: {
    color: $brand-warning
  }
}