$selector-text = '.bell-text'
$selector-texts = '.bell-texts'

$text-brand-hash = $brand-hash


$text-size-hash = $size-hash

$text-header-hash = {
    h1: 2.5rem,
    h2: 2rem,
    h3: 1.75rem,
    h4: 1.5rem,
    h5: 1.25rem,
    h6: 1rem
}

$text-display-hash = {
    display1: {
        font-size: 6rem,
        font-weight: 300,
        line-height: 1.2
    },
    display2: {
        font-size: 5.5rem,
        font-weight: 300,
        line-height: 1.2
    },
    display3: {
        font-size: 4.5rem,
        font-weight: 300,
        line-height: 1.2
    },
    display4: {
        font-size: 3.5rem,
        font-weight: 300,
        line-height: 1.2
    }
}

// 文本对齐
$text-alignment-hash = {
    left: left,
    center: center
    right: right
}
