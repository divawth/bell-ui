$selector-loading-bar-wrapper = '#bell-loading-bar-wrapper';
$selector-loading-bar = '.bell-loading-bar';
$selector-loading-bar-bg = '.bell-loading-bar-bg';


$loading-bar-inner-background-color = #f8f8f8;

$loading-bar-brand-hash = {
    '': {
        bg-color: $brand-primary
    },
    'success': {
        bg-color: $brand-success,
    },
    'error': {
        bg-color: $brand-error,
    },
    'info': {
        bg-color: $brand-info,
    },
    'warning': {
        bg-color: $brand-warning,
    },
    'danger': {
        bg-color: $brand-danger,
    }
};