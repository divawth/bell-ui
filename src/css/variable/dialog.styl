$selector-dialog = $selector-prefix + '-dialog'

$selector-dialog-visible = $selector-dialog + '-visible'

$selector-dialog-mask = $selector-dialog + '-mask'
$selector-dialog-wrapper = $selector-dialog + '-wrapper'

$selector-dialog-title = $selector-dialog + '-title'
$selector-dialog-content = $selector-dialog + '-content'
$selector-dialog-footer = $selector-dialog + '-footer'
$selector-dialog-close = $selector-dialog + '-close'
$selector-dialog-close-icon = $selector-dialog + '-close-icon'


$dialog-padding-horizontal = 24px

$dialog-wrapper-bg-color = $brand-white

$dialog-title-padding-vertical = 16px
$dialog-content-padding-vertical = 24px
$dialog-footer-padding-vertical = 10px
$dialog-footer-padding-horizontal = 16px

$dialog-close-font-size = 30px
$dialog-close-top = 9px
$dialog-close-right = 14px
