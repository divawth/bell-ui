$selector-tabs = $selector-prefix + '-tabs'
$selector-tabs-card = $selector-tabs + '-card'
$selector-tabs-bar = $selector-tabs + '-bar'
$selector-tabs-nav-container = $selector-tabs + '-nav-container'
$selector-tabs-nav-closable = $selector-tabs + '-nav-closable'
$selector-tabs-extra = $selector-tabs + '-extra'
$selector-tabs-nav = $selector-tabs + '-nav'
$selector-tabs-tab = $selector-tabs + '-tab'
$selector-tabs-tab-wrapper = $selector-tabs-tab + '-wrapper'
$selector-tabs-tab-scroll = $selector-tabs-tab + '-scroll'
$selector-tabs-tab-active = $selector-tabs-tab + '-active'
$selector-tabs-tab-enabled = $selector-tabs-tab + '-enabled'
$selector-tabs-tab-disabled = $selector-tabs-tab + '-disabled'
$selector-tabs-tab-icon = $selector-tabs-tab + '-icon'
$selector-tabs-tab-close-icon = $selector-tabs-tab + '-close-icon'
$selector-tabs-content = $selector-tabs + '-content'
$selector-tabs-panel = $selector-tabs + '-panel'
$selector-tabs-panel-active = $selector-tabs-panel + '-active'
$selector-tabs-panel-disabled = $selector-tabs-panel + '-disabled'


$tabs-nav-content-gap = 16px
$tabs-close-size = 22px
$tabs-card-bg-color = #fafafa
$tabs-card-bg-color-active = $brand-white

$tabs-bar-size-hash = {
  default: {
    padding-horizontal: 20px,
    font-size: 14px,
    height: 40px
  },
  small: {
    padding-horizontal: 16px,
    font-size: 14px,
    height: 32px
  },
  large: {
    padding-horizontal: 24px,
    font-size: 16px,
    height: 48px
  }
}