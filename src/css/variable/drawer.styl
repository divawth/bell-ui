$selector-drawer = $selector-prefix + '-drawer'
$selector-drawer-wrapper = $selector-drawer + '-wrapper'
$selector-drawer-header = $selector-drawer + '-header'
$selector-drawer-body = $selector-drawer + '-body'
$selector-drawer-mask = $selector-drawer + '-mask'
$selector-drawer-close = $selector-drawer + '-close'

$selector-drawer-top = $selector-drawer + '-top'
$selector-drawer-right = $selector-drawer + '-right'
$selector-drawer-bottom = $selector-drawer + '-bottom'
$selector-drawer-left = $selector-drawer + '-left'
$selector-drawer-visible = $selector-drawer + '-visible'
$selector-drawer-fade = $selector-drawer + '-fade'


$drawer-padding-horizontal = 24px
$drawer-header-padding-vertical = 16px
$drawer-body-padding-vertical = 16px

$drawer-header-font-size = $font-size-large
$drawer-body-font-size = $font-size-default

$drawer-close-top = 9px
$drawer-close-right = 15px
$drawer-close-font-size = 30px
