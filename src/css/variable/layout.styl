$selector-layout = $selector-prefix + '-layout'
$selector-layout-horizontal = $selector-layout + '-horizontal'
$selector-layout-vertical = $selector-layout + '-vertical'
$selector-layout-fixed = $selector-layout + '-fixed'

$selector-layout-header = $selector-layout + '-header'
$selector-layout-header-extra = $selector-layout-header + '-extra'

$selector-layout-sider = $selector-layout + '-sider'
$selector-layout-content = $selector-layout + '-content'
$selector-layout-footer = $selector-layout + '-footer'

$selector-layout-sider-with-trigger = $selector-layout-sider + '-with-trigger'
$selector-layout-sider-logo = $selector-layout-sider + '-logo'
$selector-layout-sider-collapsed = $selector-layout-sider + '-collapsed'
$selector-layout-sider-trigger = $selector-layout-sider + '-trigger'
$selector-layout-sider-trigger-icon = $selector-layout-sider-trigger + '-icon'

$layout-bg-color = #f0f2f5
$layout-padding-horizontal = 24px
$layout-header-height = 64px
$layout-sider-trigger-height = 48px
$layout-sider-trigger-icon-size = 22px

$layout-dark-bg-color = #515a6e
$layout-light-bg-color = $brand-white