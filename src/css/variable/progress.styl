$selector-progress = '.bell-progress';
$selector-progress-vertical = '.bell-progress-vertical';
$selector-progress-outer = '.bell-progress-outer';
$selector-progress-inner = '.bell-progress-inner';
$selector-progress-inner-text = '.bell-progress-inner-text';
$selector-progress-bg = '.bell-progress-bg';


$progress-inner-text-width = 60px;
$progress-inner-text-font-size = 12px;
$progress-inner-border-radius = 100px;

$progress-brand-hash = {
    '': {
        bg-color: $brand-primary
    },
    'success': {
        bg-color: $brand-success
    },
    'error': {
        bg-color: $brand-error
    },
    'warning': {
        bg-color: $brand-warning
    },
    'danger': {
        bg-color: $brand-danger
    },
    'info': {
        bg-color: $brand-info
    }
};