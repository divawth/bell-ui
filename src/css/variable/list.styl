$selector-list = $selector-prefix + '-list'
$selector-list-simple = $selector-list + '-simple'
$selector-list-bordered = $selector-list + '-bordered'
$selector-list-header = $selector-list + '-header'
$selector-list-empty-desc = $selector-list + '-empty-desc'
$selector-list-item = $selector-list + '-item'
$selector-list-item-clickable = $selector-list-item + '-clickable'
$selector-list-item-active = $selector-list-item + '-active'
$selector-list-item-header = $selector-list-item + '-header'
$selector-list-item-content = $selector-list-item + '-content'
$selector-list-item-footer = $selector-list-item + '-footer'
$selector-list-item-text = $selector-list-item + '-text'

$list-size-hash = {
  default: {
    header-padding: 12px 16px,
    padding-horizontal: 16px,
    padding-vertical: 12px,
  },
  large: {
    header-padding: 14px 20px,
    padding-horizontal: 20px,
    padding-vertical: 16px,
  },
  small: {
    header-padding: 10px 14px,
    padding-horizontal: 14px,
    padding-vertical: 8px,
  }
}
