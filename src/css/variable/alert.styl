$selector-alert = $selector-prefix + '-alert'
$selector-alert-center = $selector-alert + '-center'
$selector-alert-banner = $selector-alert + '-banner'

$selector-alert-with-icon = $selector-alert + '-with-icon'
$selector-alert-with-close = $selector-alert + '-with-close'
$selector-alert-with-title = $selector-alert + '-with-title'

$selector-alert-close = $selector-alert + '-close'
$selector-alert-title = $selector-alert + '-title'
$selector-alert-content = $selector-alert + '-content'

$selector-alert-status-icon = $selector-alert + '-status-icon'
$selector-alert-close-icon = $selector-alert + '-close-icon'

$alert-padding-horizontal = 16px
$alert-padding-vertical = 9px

$alert-title-content-gap = 2px

$alert-close-height = 21px
$alert-close-font-size = 12px

$alert-status-icon-gap = 8px
$alert-status-icon-size = 16px
$alert-status-icon-top = 2px

$alert-status-icon-gap-with-title = 12px
$alert-status-icon-size-with-title = 22px
$alert-status-icon-top-with-title = 2px

$alert-brand-hash = {
  info: {
    border-color: $brand-info-border,
    bg-color: $brand-info-bg,
    color: $brand-info,
  },
  success: {
    border-color: $brand-success-border,
    bg-color: $brand-success-bg,
    color: $brand-success,
  },
  error: {
    border-color: $brand-error-border,
    bg-color: $brand-error-bg,
    color: $brand-error,
  },
  warning: {
    border-color: $brand-warning-border,
    bg-color: $brand-warning-bg,
    color: $brand-warning,
  },
}
