{$selector-dropdown}
  display inline-block
  position relative

{$selector-dropdown-menu}
  min-width $dropdown-menu-min-width

{$selector-dropdown-item}
  ui-ellipsis()
  padding $dropdown-item-padding-vertical $dropdown-item-padding-horizontal
  transition background-color .3s, color .3s

{$selector-dropdown-item-active}
  background-color $brand-bg-active

{$selector-dropdown-item-enabled}
  color $brand-text
  cursor $cursor-clickable

  &:hover,
  &{$selector-dropdown-item-hover}
    background-color $brand-bg-hover

{$selector-dropdown-item-disabled}
  color $brand-text-disabled
  cursor $cursor-disabled

{$selector-dropdown-item} + {$selector-dropdown-item-divided}
  margin-top $dropdown-list-inset
  border-top 1px solid $brand-divider
  padding-top $dropdown-item-padding-vertical + $dropdown-list-inset
  position relative

  &:before
    content " "
    background-color $brand-bg
    position absolute
    top 0
    left 0
    right 0
    height $dropdown-list-inset

{$selector-dropdown-list}
  font-size $dropdown-item-size
  line-height $ui-single-line-height
  padding $dropdown-list-inset 0

  overflow-y auto
  overflow-x hidden
  user-select none
  box-sizing border-box

  position absolute
  display none

  z-index $zindex-dropdown
  transition transform .3s cubic-bezier(.23,1,.32,1),
              opacity .3s cubic-bezier(.23,1,.32,1)

  background-color $brand-bg
  border-radius $radius-base
  box-shadow $dropdown-list-shadow

{$selector-dropdown-visible}
  > {$selector-dropdown-list}
    display block
    opacity 0

{$selector-dropdown-fade}
  > {$selector-dropdown-list}
    opacity 1

{$selector-select-top},
{$selector-dropdown}-top,
{$selector-dropdown}-top-start,
{$selector-dropdown}-top-end
  > {$selector-dropdown-list}
    bottom 100%
    margin-bottom $dropdown-trigger-list-gap
    transform scaleY(0)
    transform-origin center bottom

  &{$selector-dropdown-fade}
    > {$selector-dropdown-list}
      transform scaleY(1)

{$selector-dropdown}-top
  > {$selector-dropdown-list}
    left 50%

{$selector-dropdown}-top-start
  > {$selector-dropdown-list}
    left 0

{$selector-dropdown}-top-end
  > {$selector-dropdown-list}
    right 0




{$selector-dropdown}-left,
{$selector-dropdown}-left-start,
{$selector-dropdown}-left-end
  > {$selector-dropdown-list}
    right 100%
    margin-right $dropdown-trigger-list-gap
    transform scaleX(0)
    transform-origin right center

  &{$selector-dropdown-fade}
    > {$selector-dropdown-list}
      transform scaleX(1)

{$selector-dropdown}-left
  > {$selector-dropdown-list}
    top 50%

{$selector-dropdown}-left-start
  > {$selector-dropdown-list}
    top 0

{$selector-dropdown}-left-
  > {$selector-dropdown-list}
    bottom 0




{$selector-dropdown}-right,
{$selector-dropdown}-right-start,
{$selector-dropdown}-right-end
  > {$selector-dropdown-list}
    left 100%
    margin-left $dropdown-trigger-list-gap
    transform scaleX(0)
    transform-origin left center

  &{$selector-dropdown-fade}
    > {$selector-dropdown-list}
      transform scaleX(1)

{$selector-dropdown}-right
  > {$selector-dropdown-list}
    top 50%

{$selector-dropdown}-right-start
  > {$selector-dropdown-list}
    top 0

{$selector-dropdown}-right-end
  > {$selector-dropdown-list}
    bottom 0





{$selector-select-bottom},
{$selector-dropdown}-bottom,
{$selector-dropdown}-bottom-start,
{$selector-dropdown}-bottom-end
  > {$selector-dropdown-list}
    margin-top $dropdown-trigger-list-gap
    transform scaleY(0)
    transform-origin center top

  &{$selector-dropdown-fade}
    > {$selector-dropdown-list}
      transform scaleY(1)

{$selector-dropdown}-bottom
  > {$selector-dropdown-list}
    left 50%

{$selector-dropdown}-bottom-start
  > {$selector-dropdown-list}
    left 0

{$selector-dropdown}-bottom-end
  > {$selector-dropdown-list}
    right 0