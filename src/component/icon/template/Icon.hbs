<i
class="${prefix}icon
{{#if name}} ${prefix}icon-{{name}}{{/if}}
{{#if spin}} ${prefix}icon-spin{{/if}}
{{#if className}} {{className}}{{/if}}"

{{#if inlineStyle}}
  style="{{inlineStyle}}"
{{/if}}
>
  <slot />
</i>