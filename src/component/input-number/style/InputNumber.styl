@import '../../../env'

$input-number-size-hash = {
  default: {
    height: $height-default,
    actions-width: 22px,
  },
  small: {
    height: $height-small,
    actions-width: 20px,
  }
  large: {
    height: $height-large,
    actions-width: 26px,
  }
}

input-number-size-style(style)

  {$selector-class-prefix + '-input-number-actions'}
    ui-set('width', style['actions-width'])

  {$selector-class-prefix + '-input-number-up'},
  {$selector-class-prefix + '-input-number-down'}
    ui-set('line-height', 0.5 * (style['height'] - 2))


{$selector-class-prefix + '-input'}
  ui-connect(
    hash: $input-number-size-hash,
    each: input-number-size-style
  )


{$selector-class-prefix + '-input-number'}
  > {$selector-class-prefix + '-input-content'}
    display flex
    flex-direction row
    align-items center

{$selector-class-prefix + '-input-number-el'}
  display none

{$selector-class-prefix + '-input-number-actions'}
  position relative
  height 100%
  opacity 0
  user-select none
  transition opacity .3s
  box-sizing border-box
  border-left 1px solid $brand-border

{$selector-class-prefix + '-input-number-up'},
{$selector-class-prefix + '-input-number-down'}
  position absolute
  left 0
  width 100%
  height 50%
  font-size $ui-icon-size
  text-align center
  background-color $brand-bg-light
  box-sizing border-box
  transition color .3s

{$selector-class-prefix + '-input-number-up'}
  top 0
  border-top-right-radius $border-radius-default

{$selector-class-prefix + '-input-number-down'}
  bottom 0
  border-top 1px solid $brand-border
  border-bottom-right-radius $border-radius-default

{$selector-class-prefix + '-input-with-append'}
  {$selector-class-prefix + '-input-number-up'}
    border-top-right-radius 0
  {$selector-class-prefix + '-input-number-down'}
    border-bottom-right-radius 0

{$selector-class-prefix + '-input-number-up-icon'},
{$selector-class-prefix + '-input-number-down-icon'}
  line-height inherit

{$selector-class-prefix + '-input-number-up-enabled'},
{$selector-class-prefix + '-input-number-down-enabled'}
  color $brand-close-color
  ui-clickable()

  &:hover
    color $brand-close-color-hover

{$selector-class-prefix + '-input-number-up-disabled'},
{$selector-class-prefix + '-input-number-down-disabled'}
  color $brand-disabled
  ui-disabled()

{$selector-class-prefix + '-input-enabled'}:hover

  {$selector-class-prefix + '-input-number-actions'}
    opacity 1

