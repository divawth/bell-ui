@import '../../../env'

$divider-horizontal-gap = 12px
$divider-vertical-gap = 8px
$divider-text-gap = 10px

{$selector-class-prefix + '-divider'}
  box-sizing border-box
  background-color $brand-divider

{$selector-class-prefix + '-divider-horizontal'}
  display block
  clear both
  width 100%
  height 1px
  margin $divider-horizontal-gap 0

{$selector-class-prefix + '-divider-vertical'}
  display inline-block
  vertical-align middle
  position relative
  width 1px
  height .9em
  top -.06em
  margin 0 $divider-vertical-gap

{$selector-class-prefix + '-divider-text'}
  margin 0 $divider-text-gap

{$selector-class-prefix + '-divider-dashed'}
  background-color transparent
  border-top 1px dashed $brand-divider

{$selector-class-prefix + '-divider-with-text'}

  ui-table-faker()
  white-space nowrap
  background-color transparent
  border-top 0

  color $brand-title
  font-size $font-size-default
  font-weight 500

  &:before,
  &:after
    content ''
    display table-cell
    border-top 1px solid $brand-divider
    position relative
    top 50%
    width 50%

  &{$selector-class-prefix + '-divider-dashed'}
    &:before,
    &:after
      border-top-style dashed

  &{$selector-class-prefix + '-divider-left'}
    &:before
      width 5%
    &:after
      width 95%

  &{$selector-class-prefix + '-divider-right'}
    &:before
      width 95%
    &:after
      width 5%