@import '../../../env'

$split-button-menu-height = 200px

{$selector-prefix + '-split-button-menu'}
  min-width $dropdown-menu-min-width
  max-height $split-button-menu-height

ui-dropdown-overlay(
  $selector-prefix + '-split-button-menu',
  $selector-prefix + '-split-button-menu-item',
  '',
  $selector-prefix + '-split-button-menu-item-enabled',
  $selector-prefix + '-split-button-menu-item-disabled',
  '',
  $brand-primary,
  $brand-bg-active
)