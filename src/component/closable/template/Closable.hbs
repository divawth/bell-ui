<div
class="${prefix}closable
{{#if showClose === RAW_ALWAYS}} ${prefix}closable-show-always
{{else if showClose === RAW_HOVER}} ${prefix}closable-show-hover
{{/if}}
{{#if className}} {{className}}{{/if}}"

{{#if draggable}}
  draggable
{{/if}}

{{#if style}}
  style="{{style}}"
{{/if}}
>
  <slot />
  {{#if needClose}}
    <Icon
      className="${prefix}closable-icon"
      name="close-circle-fill"
      on-click.native="close.closable"
    />
  {{/if}}
</div>