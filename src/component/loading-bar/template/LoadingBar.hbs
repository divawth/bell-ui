<div
class="${prefix}loadingbar
{{#if type}} ${prefix}loadingbar-{{type}}{{/if}}"
>
  <div
    class="${prefix}loadingbar-indicator"
    style="{{ {
      width: percent + '%',
      height: height + 'px',
      color: color || undefined
    } }}"
  ></div>
</div>