@import '../../../env'

{$selector-prefix + '-circle'}
  display inline-block
  position relative

{$selector-prefix + '-circle-content'}
  width 100%
  position absolute
  left 50%
  top 50%
  text-align center
  transform translate(-50%,-50%)
  line-height $ui-single-line-height