@import '../../../env'

$message-padding-vertical = 8px
$message-padding-horizontal = 16px

$message-icon-size = 18px
$message-close-icon-right = 12px
$message-close-icon-top = 12px

message-brand-style(style)
  > {$selector-class-prefix + '-message-icon'}
    ui-set('color', style['color'])

{$selector-class-prefix + '-message'}
  position fixed
  top 0
  left 50%
  opacity 0
  z-index $zindex-message
  transform translateX(-50%)

  user-select none
  white-space nowrap
  background-color $brand-bg
  padding $message-padding-vertical $message-padding-horizontal
  color $brand-content
  font-size $font-size-default
  line-height $ui-multiple-line-height
  border-radius $border-radius-default
  box-shadow $brand-message-shadow
  transition opacity .15s, top .3s

  ui-connect(
    hash: $status-brand-hash,
    each: message-brand-style
  )

{$selector-class-prefix + '-message-with-close'}
  padding-right $message-close-icon-right + $message-close-icon-size + $ui-icon-text-gap

{$selector-class-prefix + '-message-visible'}
  opacity 1

{$selector-class-prefix + '-message-center'}
  text-align center

{$selector-class-prefix + '-message-icon'}
  font-size $message-icon-size
  margin-right $ui-icon-text-gap
  vertical-align middle

{$selector-class-prefix + '-message-text'}
  vertical-align middle

{$selector-class-prefix + '-message-close'}
  position absolute
  right $message-close-icon-right
  top $message-close-icon-top

  color $brand-close-color
  font-size $message-close-icon-size
  transition color $form-control-transition-duration
  ui-clickable()

  &:hover
    color $brand-close-color-hover
