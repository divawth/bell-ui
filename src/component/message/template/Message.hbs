<div
class="${prefix}message
{{#if status}} ${prefix}message-{{status}}{{/if}}
{{#if center}} ${prefix}message-center{{/if}}
{{#if closable}} ${prefix}message-with-close{{/if}}"
>
  <Icon
    name="{{
      status === RAW_TYPE_SUCCESS
        ? 'checkbox-circle-fill'
        : status === RAW_TYPE_WARNING
          ? 'error-warning-fill'
          : status === RAW_TYPE_ERROR
            ? 'close-circle-fill'
            : 'information-fill'
    }}"
    size="0"
    className="${prefix}message-icon"
  />

  <span class="${prefix}message-text">
    {{content}}
  </span>

  {{#if closable}}
    <Icon
      name="close-line"
      size="0"
      className="${prefix}message-close"
      on-click.native="hide()"
    />
  {{/if}}
</div>