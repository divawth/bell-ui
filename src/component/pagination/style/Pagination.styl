@import '../../../env'

$pagination-simple-input-width = 55px
$pagination-jumper-input-width = 100px
$pagination-select-width = 100px
$pagination-ellipsis-width = 22px

$pagination-size-hash = {
  default: {
    height: $height-default,
    font-size: $font-size-default,
    padding-horizontal: 8px,
  },
  small: {
    height: $height-small,
    font-size: $font-size-small,
    padding-horizontal: 6px,
  },
}

pagination-size-style(style)
  ui-set('font-size', style['font-size'])

  {$selector-class-prefix + '-pagination-item'}
    ui-set('min-width', style['height'])
    ui-set('height', style['height'])
    ui-set('line-height', style['height'])
    ui-set('font-size', style['font-size'])
    ui-set('padding', 0 style['padding-horizontal'])

{$selector-class-prefix + '-pagination-list'}
  display inline-block

{$selector-class-prefix + '-pagination-hidden'}
  display none

{$selector-class-prefix + '-pagination-item'},
{$selector-class-prefix + '-pagination-custom-prev'},
{$selector-class-prefix + '-pagination-custom-next'}
  display inline-block
  text-align center
  color $brand-content
  ui-clickable()

{$selector-class-prefix + '-pagination-item'}
  outline 0
  padding 0
  border-radius $border-radius-default
  border 1px solid $brand-border
  background-color $brand-bg
  transition color .2s, border-color .2s, box-shadow .2s, background-color .2s
  box-sizing border-box

  + {$selector-class-prefix + '-pagination-item'}
    margin-left $ui-horizontal-gap

{$selector-class-prefix + '-pagination-item-disabled'}
  color $brand-disabled
  background-color $brand-bg-disabled
  ui-disabled(true)

{$selector-class-prefix + '-pagination-custom-prev'},
{$selector-class-prefix + '-pagination-custom-next'}
  border 0
  background-color transparent

{$selector-class-prefix + '-pagination-custom-disabled'}
  color $brand-disabled
  ui-disabled()

{$selector-class-prefix + '-pagination-item-active'}
  cursor default

{$selector-class-prefix + '-pagination-item'}:hover,
{$selector-class-prefix + '-pagination-item-active'}
  color $brand-primary
  border-color $brand-primary

  &{$selector-class-prefix + '-pagination-item-disabled'}
    color $brand-disabled
    border-color $brand-border

{$selector-class-prefix + '-pagination-custom-prev'} + {$selector-class-prefix + '-pagination-item'},
{$selector-class-prefix + '-pagination-item'} + {$selector-class-prefix + '-pagination-custom-next'}
  margin-left $ui-horizontal-gap

{$selector-class-prefix + '-pagination-ellipsis'}
  display inline-block
  width $pagination-ellipsis-width
  text-align center
  margin 0  $ui-horizontal-gap
  ui-clickable()

  &:after
    content '•••'
    display block
    letter-spacing 1px
    color $brand-border
    text-align center

  > {$selector-class-prefix + '-icon'}
    color $brand-primary
    display none

  &:hover

    &:after
      display none

    > {$selector-class-prefix + '-icon'}
      display inline-block




{$selector-class-prefix + '-pagination'}
  user-select none
  ui-connect(
    hash: $pagination-size-hash,
    each: pagination-size-style
  )

  {$selector-class-prefix + '-select'}
    width $pagination-select-width
    margin-right $ui-horizontal-gap
    vertical-align top

{$selector-class-prefix + '-pagination-jumper'}
  display inline-block
  margin-left $ui-horizontal-gap

{$selector-class-prefix + '-pagination-jumper-input'}
  width $pagination-jumper-input-width
  display inline-block
  vertical-align top
  margin-right $ui-icon-text-gap

{$selector-class-prefix + '-pagination-simple-input'}
  width $pagination-simple-input-width
  display inline-block
  vertical-align top

{$selector-class-prefix + '-pagination-total'}
  margin-right $ui-horizontal-gap

{$selector-class-prefix + '-pagination-simple'}
  display inline-block

  > {$selector-class-prefix + '-pagination-prev'}
    margin-right $ui-horizontal-gap

  > {$selector-class-prefix + '-pagination-next'}
    margin-left $ui-horizontal-gap

{$selector-class-prefix + '-pagination-separator'}
  color $brand-muted
  margin 0 $ui-icon-text-gap

{$selector-class-prefix + '-pagination-count'}
  color $brand-content
