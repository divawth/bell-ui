import MenuGroupTpl from './template/MenuGroup.html'

export default {
  template: MenuGroupTpl,

  propTypes: {
    className: {
      type: 'string'
    },
    style: {
      type: 'string'
    },
    title: {
      type: 'string'
    }
  }
};