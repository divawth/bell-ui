<div
class="${prefix}upload
{{#if className}} {{className}}{{/if}}"

{{#if style}}
  style="{{style}}"
{{/if}}

on-click="click()"
>
  <slot />
  <input
    ref="input"
    type="file"
    multiple="{{multiple}}"
    accept="{{accept}}"
    on-change="changeFiles()"
  >
</div>