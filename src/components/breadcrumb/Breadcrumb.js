import BreadcrumbTpl from './template/Breadcrumb.html'

export default {
  template: BreadcrumbTpl,
  propTypes: {
    className: {
      type: 'string'
    },
    style: {
      type: 'string'
    }
  }
};