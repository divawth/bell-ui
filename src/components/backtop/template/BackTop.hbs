<div
class="${prefix}backtop
{{#if className}} {{className}}{{/if}}"

style="bottom: {{bottom}}px;
right: {{right}}px;
{{#if style}} {{style}}{{/if}}"

on-click="click.backTop"
>
  <Icon
    size="0"
    name="arrow-up-line"
    className="${prefix}backtop-icon"
  />
</div>