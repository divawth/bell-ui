<div
class="${prefix}drawer
{{#if placement}} ${prefix}drawer-{{placement}}{{/if}}
{{#if className}} {{className}}{{/if}}"

{{#if style}}
  style="{{style}}"
{{/if}}
>

  {{#if mask}}
    <div
      class="${prefix}drawer-mask"
      {{#if maskClosable}}
        on-click="close()"
      {{/if}}
    ></div>
  {{/if}}

  <div
    ref="wrapper"
    class="${prefix}drawer-wrapper"
    style="{{wrapperStyle}}"
  >
    {{#if title || hasSlot('title')}}
      <div class="${prefix}drawer-header">
        <slot name="title">
          {{title}}
        </slot>
      </div>
    {{/if}}

    <div class="${prefix}drawer-body">
      <slot name="children" />
    </div>

    {{#if closable}}
      <div class="${prefix}drawer-close" on-click="close()">
        <Icon name="close" size="0" />
      </div>
    {{/if}}
  </div>

</div>